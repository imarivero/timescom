import 'package:flutter/material.dart';

class CreacionActividadScreen extends StatelessWidget {
   
  const CreacionActividadScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('CreacionActividadScreen'),
      ),
    );
  }
}